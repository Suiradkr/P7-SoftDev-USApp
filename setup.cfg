[flake8]
# PEP 8 with a slightly wider line budget, matching the formatting already
# used across the project (Black's default of 88 plus a little headroom).
max-line-length = 99
exclude =
    .venv,
    __pycache__,
    .git
